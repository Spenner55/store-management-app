//import { Link, useNavigate, useLocation } from 'react-router-dom';
import styles from './DashFooter.module.css';

const DashFooter = () => {
    const content = (
        <footer className={styles['dashFooter']}>
            Footer
        </footer>
    )
    return content;
}

export default DashFooter;